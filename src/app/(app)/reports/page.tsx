import { PlaceholderPanel } from "@/components/ui/placeholder-panel";

export default function ReportsPage() {
  return (
    <PlaceholderPanel
      title="Reports"
      description="Build operational, payroll, revenue, expense, and tax reporting views."
    />
  );
}
