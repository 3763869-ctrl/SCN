import { PlaceholderPanel } from "@/components/ui/placeholder-panel";

export default function PayrollPage() {
  return (
    <PlaceholderPanel
      title="Payroll"
      description="Prepare payroll cycles, worker earnings, deductions, and payment exports."
    />
  );
}
