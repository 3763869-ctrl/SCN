import { PlaceholderPanel } from "@/components/ui/placeholder-panel";

export default function ExpensesPage() {
  return (
    <PlaceholderPanel
      title="Expenses"
      description="Capture reimbursable costs, attachments, categories, and approval status."
    />
  );
}
