import { PlaceholderPanel } from "@/components/ui/placeholder-panel";

export default function WorkersPage() {
  return (
    <PlaceholderPanel
      title="Workers"
      description="Prepare worker profiles, assignments, compliance fields, and payroll settings."
    />
  );
}
