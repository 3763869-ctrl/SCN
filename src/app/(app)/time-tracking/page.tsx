import { PlaceholderPanel } from "@/components/ui/placeholder-panel";

export default function TimeTrackingPage() {
  return (
    <PlaceholderPanel
      title="Time Tracking"
      description="Review time entries, approvals, job allocations, and attendance workflows."
    />
  );
}
