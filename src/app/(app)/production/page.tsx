import { PlaceholderPanel } from "@/components/ui/placeholder-panel";

export default function ProductionPage() {
  return (
    <PlaceholderPanel
      title="Production (Units)"
      description="Track units, production batches, job quantities, and approval status."
    />
  );
}
