import { PlaceholderPanel } from "@/components/ui/placeholder-panel";

export default function SettingsPage() {
  return (
    <PlaceholderPanel
      title="Settings"
      description="Configure organization details, users, permissions, billing, and integrations."
    />
  );
}
