import { PlaceholderPanel } from "@/components/ui/placeholder-panel";

export default function InvoicesPage() {
  return (
    <PlaceholderPanel
      title="Invoices"
      description="Generate invoices from approved work, expenses, and production records."
    />
  );
}
